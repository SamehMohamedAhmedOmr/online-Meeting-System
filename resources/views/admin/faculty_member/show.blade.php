@extends('layouts.app')

@section('content')
    <div class="container">
        <div class="row">
            @include('admin.sidebar')

            <div class="col-md-9">
                <div class="card">
                    <div class="card-header">faculty_member {{ $faculty_member->id }}</div>
                    <div class="card-body">

                        <a href="{{ url('/admin/faculty_member') }}" title="Back"><button class="btn btn-warning btn-sm"><i class="fa fa-arrow-left" aria-hidden="true"></i> Back</button></a>
                        <a href="{{ url('/admin/faculty_member/' . $faculty_member->id . '/edit') }}" title="Edit faculty_member"><button class="btn btn-primary btn-sm"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Edit</button></a>

                        <form method="POST" action="{{ url('admin/faculty_member' . '/' . $faculty_member->id) }}" accept-charset="UTF-8" style="display:inline">
                            {{ method_field('DELETE') }}
                            {{ csrf_field() }}
                            <button type="submit" class="btn btn-danger btn-sm" title="Delete faculty_member" onclick="return confirm(&quot;Confirm delete?&quot;)"><i class="fa fa-trash-o" aria-hidden="true"></i> Delete</button>
                        </form>
                        <br/>
                        <br/>

                        <div class="table-responsive">
                            <table class="table">
                                <tbody>
                                    <tr>
                                        <th>ID</th><td>{{ $faculty_member->id }}</td>
                                    </tr>
                                    <tr><th> Member Name </th><td> {{ $faculty_member->member_name }} </td></tr><tr><th> Faculty Id </th><td> {{ $faculty_member->faculty_id }} </td></tr><tr><th> Department Id </th><td> {{ $faculty_member->department_id }} </td></tr>
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection
